export enum ProtocolEnum {
  HTTP = 'http',
  HTTPS = 'https',
  WS = 'ws',
  WSS = 'wss',
  TCP = 'tcp',
  UDP = 'udp',
  MQTT = 'mqtt',
  AMQP = 'amqp',
  RPC = 'rpc',
  GRPC = 'grpc',
  GRAPHQL = 'graphql',
  REST = 'rest',
}
