# Feature: View Session Calendar

- Controller: `ScheduleController.getScheduleByCourse(courseId)`
- Service: `ScheduleService.getScheduleByCourse(courseId)`
- Entities: `Schedule`, `Course`
- Scope: Only read methods used by sequence.
