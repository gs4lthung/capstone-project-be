# Feature: Approve Course (Planned)

- Controller: `RequestController.approve(id)`, `RequestController.reject(id)`
- Service: `RequestService.approve(id)`, `RequestService.reject(id)`
- Entities: `Request`, `Course`
- Status: FUTURE — endpoints to be implemented per audit.
