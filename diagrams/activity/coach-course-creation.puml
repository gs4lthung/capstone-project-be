@startuml Coach Course Creation Workflow

title Coach Course Creation Workflow

start

:Coach Login;
if (Valid credentials?) then (yes)
  :Access Dashboard;
  :Navigate to Create Course;
  :Select Subject;
  :Enter Course Details;
  :Submit Course Request;
  :Course status = PENDING_APPROVAL;
  :Receive confirmation;
  
else (no)
  :Show Error;
  stop
endif

:System sends notification to Admin;
:Admin reviews course;

if (Admin approves?) then (yes)
  :Course status = APPROVED;
  :Notify Coach;
  
  :Coach prepares course content;
  :Create Subject (if needed);
  :Create Lessons;
  loop For each lesson
    :Add Quiz to Lesson;
    :Add Video to Lesson;
  end loop
  
  :Review all content;
  :Publish Course;
  :Course status = READY_OPENED;
  :Available for enrollment;
  
else (no)
  :Course status = REJECTED;
  :Notify Coach with rejection reason;
  :Coach can edit and resubmit;
endif

stop
